<h2>Catalogo Eventi</h2>
<div class="sidebar">
    <form method="post" action="#" id="search_form">
          <input class="search" type="text" name="search_field" placeholder="Cerca...">
        </form>
</div>

    
        <?php foreach ($this->Eventi as $type){ 
          echo "<div class=tabella><table class=elenco><th colspan=2>$type->nome</th>";
          $more=$this->url(array(
                        'controller' => 'public',
                        'action'     => 'evento',
                        ), 
                        'default',true
                    );
	  echo "<tr><td><img src=$type->immagine></td>"; 
          echo "<td><p>$type->descrizione</p>";
          echo "Prezzo: $type->prezzo";
          echo "<br>Luogo: $type->luogo"; 
          echo "<br>Data: $type->data";
          echo "<br>Orario di inizio: $type->orario</p>";
          echo "<a href=$more>Clicca qui per saperne di più</a></td></tr></table></div>";}
          ?>

<?php if($this->products instanceof Zend_Paginator) {
    echo $this->paginationControl($this->tipologia,'Sliding','public/_paginator.phtml');
}